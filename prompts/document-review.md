Read the contents of the file using `read_file`. Then correct grammar (including
spelling and awkward phrasing) and LaTeX coherence, adhering to standard
terminology and ensuring math blocks align with the text.

Don't output corrections, write them directly with `edit_file`.

The file is [FILE]
